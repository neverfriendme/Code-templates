function start() {
            document.getElementById("text").innerHTML = "That was obvious"
            const a = prompt("???: ")
            if (a === "Yes") {
                        document.getElementsByname("Python").innerHTML = "-... ..- - / .. ... / - .... .. ... ..--.."
            }
        }
